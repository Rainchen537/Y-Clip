# Y-Clip AI Changelog

本文件记录 Y-Clip 面向 AI/维护者的工作流水。每次功能、修复、发布、发布事故处理或维护规范调整后追加记录。不要在这里记录 token、证书密码、Apple ID、notary 密码等敏感信息。

旧的 `UPDATE_LOG.local.md` 内容已迁移到本文件，后续维护记录统一写这里。

## 2026-07-22

- 准备安全补丁 `v1.0.19 (20)`：自动更新保存所选 GitHub Release 的 expectedVersion，并在下载前、挂载准备前和安装脚本中再次要求它严格高于当前版本；挂载 App、prepared copy、同卷 candidate 和最终 destination 的 `CFBundleShortVersionString` 都必须与 expectedVersion 完全一致，旧包改名、同版本、降级或异常版本会安全停止。
- 更新安装脚本使用 `/Applications` 同卷 candidate + backup + 原子 rename，挂载源、candidate、现有 App 与最终 App 都验证非 symlink 身份、Bundle ID、Developer ID 团队与 authority、hardened runtime、严格 codesign、Gatekeeper、版本和当前编译架构唯一 thin slice；候选复制、落位、最终验证或后续兼容副本处理失败时恢复原 App。
- 根目录隐藏兼容路径 `/Applications/Global Clipboard.app` 只有通过相同身份验证后才移动到随机 quarantine 路径并再次验证后删除；未知目录、错误 Bundle ID 或 symlink 会原样保留并停止更新。
- 更新事务增加固定 `/Applications/.Y-Clip-update.lock`；installer 使用 `/bin/zsh -f`，写出 `READY\n` 后关闭 stdout，Swift `poll(2)` 在 60 秒内持续读取到 EOF 并要求完整帧严格相等。`READY\nCORRUPT`、不关闭 pipe、短帧、timeout 或取消都会终止 installer；TERM/INT/HUP 在父进程等待阶段撤销退出授权并执行 cleanup，在 candidate/primary/legacy 的原子 rename 或 swap 与状态变量同步期间则只记录 pending，待 shell 状态准确反映真实磁盘布局后再退出并回滚，避免未提交 expectedVersion 留在 canonical path 后释放锁。
- 新版 App 启动时以随机 UUID token 和状态文件写入 `STARTING <token> <pid>`，`applicationDidFinishLaunching` 完成后再写 `READY`；installer 同时核对 token、PID、`ps` 的精确 destination 可执行路径，并在 READY 后继续确认存活。事务只有通过该确认才标记 committed；失败恢复只允许启动内部版本等于 `CURRENT_VERSION` 的已验证副本。初次 rollback swap 失败时 cleanup 会再次尝试；若持续失败，则不得启动未通过 READY 的 expectedVersion，也不得释放固定锁或删除 source、DMG、installer、backup/candidate/quarantine 恢复现场。
- 嵌入 installer 通过 `zsh -n`；受控临时目录事务 harness 扩展为 36 个场景并全部通过。除“新版 READY 后在存活确认期退出 + rollback swap 一次失败/持续失败”外，新增 8 个原子操作后信号注入：legacy-only candidate→destination 分别注入 TERM/INT/HUP，以及 primary swap、legacy quarantine、rollback swap、legacy restore、new-primary rollback 分别注入 TERM；确认未提交 expectedVersion 不会留在 canonical path，完整恢复会只启动 currentVersion 并释放锁，恢复不完整仍保留锁与全部副本。另从两份 Swift 源码提取实际 READY reader 编译测试，`READY\n`+EOF、尾随字节、开放 pipe、短帧和空帧共 10/10 通过；这些 harness 位于受控临时目录、未持久化进仓库，且未读取用户剪贴板或触碰真实 `/Applications`。
- `SoftwareUpdateAssetSelector` 测试新增下载 App 内部版本精确匹配以及同版本、降级和异常版本拒绝；测试通过，最新 updater 修改后的 arm64 与 x86_64 隔离 ad-hoc thin App 已重新构建为 `1.0.19 (20)`，正式发布仍需从最终提交重新生成。
- `release.sh` 的源码指纹改为 fail closed：确认独立 Git 根、显式检查 tracked/untracked NUL 枚举、直接写入 symlink 原始 target 字节（保留末尾换行）、为 tracked 工作区删除写入稳定 marker，并通过 stdin 计算普通文件 SHA-256，使反斜杠或换行文件名也能稳定进入摘要；baseline 移到资产测试前，完整 `.new` 双包验证后切换前及最终产物验证后成功标记前再次复核。正式发布还在 baseline 前要求仓库内非 symlink 的 `Y-Framework` 共同祖先以及 Setting、Permission、DMG vendored 子树，并以 `Y_RELEASE_REQUIRE_VENDORED=1` 禁止 build/DMG 回退到父目录源码；强制模式固定使用仓库内背景生成器并清除外部布局变量。三项目指纹、异常文件名与 vendored 输入 harness 均通过，vendored harness 15/15 覆盖外部 generator、父目录 fallback、DMG symlink 和祖先 symlink。
- 从最终发布源码提交 `757de76` 重新运行 `release.sh` 成功：arm64 与 x86_64 的 App、隐藏兼容 App 和 DMG 均分别完成 Developer ID 签名、公证、staple、Gatekeeper、`hdiutil verify`、最终只读挂载、版本/build 与精确 thin 架构复核。唯一最终 arm64 DMG 为 `2,876,830` bytes，SHA256 `3ff9f60246e7214d50ae210a63560b1cfefd113d8da4f3581093ec469123ef37`；x86_64 DMG 为 `2,911,468` bytes，SHA256 `a8b8a7cf35629bafb20c8a65dd4e2c2ac77f0a2db63234e2cd4391de0867b048`；修复信号窗口前生成的同版本 DMG 已作废且未上传。
- 从上述最终 arm64 DMG 以可回滚方式覆盖安装 `/Applications/Y-Clip.app`，确认 `1.0.19 (20)`、Team ID `A94225N8T5`、严格 codesign、Gatekeeper 与 arm64 thin 架构通过并恢复后台运行；x86_64 最终 DMG 中严格 thin 的可执行文件通过 Rosetta 短暂启动存活冒烟后已终止，临时实例、镜像和挂载点已清理。
- 发布源码提交和 tag `v1.0.19` 已推送；GitHub Draft Release 严格先上传并核对 arm64，再上传 x86_64。Release `https://github.com/Rainchen537/Y-Clip/releases/tag/v1.0.19` 已公开，latest-release API 确认恰好包含上述两份 DMG，arm64 排在首位，远端名称、顺序、bytes、SHA-256 digest 与本地最终文件一致；远端 tag 精确解析到 `757de76`，后续维护记录提交不移动 tag。
- 准备 `v1.0.18 (19)` 双架构发布支持：`build.sh` 仅接受 `TARGET_ARCH=arm64|x86_64`，默认 arm64，并通过 `BUILD_DIR` / `APP_PATH` 隔离输出；编译后和最终 App 都严格断言为目标 thin binary。日常构建默认使用 ad-hoc 签名，不访问钥匙串证书。
- `make_dmg.sh` 新增 App 名称/路径、卷名和输出路径 override；`release.sh` 从全新临时目录为两个架构分别独立 build/stage，逐一完成 App 与 DMG 的签名、公证、staple、Gatekeeper、`hdiutil` 和挂载验证，最终目标文件名为 `Y-Clip-v1.0.18-arm64.dmg` 与 `Y-Clip-v1.0.18-x86_64.dmg`。
- 自动更新改为按编译架构精确匹配完整资产名，不再选择资产列表中的第一个 `.dmg`；缺少当前架构资产时保持安全失败并打开对应 Release 页面。下载 DMG 挂载定位 App 后、复制到临时目录后及替换脚本移动旧 App 前，都会检查 `GlobalClipboard` 主可执行文件只能包含当前编译架构；架构不匹配、反向架构或 universal binary 均安全失败并保留现有 App。新增独立资产与 thin 架构判断测试，覆盖资产顺序、无关 DMG、缺失架构、架构不匹配及 universal 拒绝。
- 使用 ad-hoc 签名分别完成 arm64 与 x86_64 隔离构建，两个 App 均为 `1.0.18 (19)`、通过严格 codesign 且 `lipo` 仅包含目标架构；资产/thin 架构选择测试及非法 `TARGET_ARCH` 白名单测试通过。另以 override 为两个架构生成临时测试 DMG，确认自定义卷名/输出路径生效，`hdiutil verify` 通过，挂载后的可见 App 与隐藏兼容副本均保持对应 thin 架构；测试镜像和挂载点已清理。无凭据 release 预检顺序测试确认资产测试先于公证 profile 查询；双 DMG 发布事务模拟确认中途失败时能够恢复完整旧资产组，原先没有资产时也不会留下半套新文件。
- `release.sh` 在任何签名、公证和凭据预检前先执行 `test_asset_selection.sh`；最终 dist 发布先在同一目录准备并验证两个新 DMG，再成套切换，普通失败会删除本轮新文件并恢复切换前的完整旧资产组，不再预先删除已有 dist。新增同版本原子目录锁，拒绝两个发布进程交错覆盖或误删彼此资产；同时记录仓库源码指纹并在每个架构构建前后及最终切换前复核，中途源码发生变化时停止发布，避免双包混入不同源码。异常退出会保留锁以阻止后续流程静默覆盖现场。
- 首次双资产迁移 `v1.0.18` 的上传顺序明确为 arm64 在前、x86_64 在后；发布后必须验证 GitHub latest API 返回的首个 `.dmg` 是 arm64，以兼容仍选择第一个 `.dmg` 的 `v1.0.17` 旧更新器。本次只发布两个 thin 包，不新增 universal 包。
- 从发布源码提交 `853b9ae` 重新运行最终 `release.sh` 成功：arm64 与 x86_64 的 App 和 DMG 均独立完成 Developer ID 签名、公证、staple、Gatekeeper、`hdiutil verify`、最终只读挂载、可见 App/隐藏兼容 App 签名与精确 thin 架构复核。唯一最终 arm64 DMG 为 `2,834,767` bytes，SHA256 `3910bedef18b526bc2b6b3b3e76ea2d5f53636c86913fbcde124486ce967792c`；x86_64 DMG 为 `2,864,430` bytes，SHA256 `68783a42a216662872876dcc5950c45191ac6320468d4eb4dadde70f9affe643`；此前重新生成前的摘要已作废，不得用于上传或校验。
- 从上述最终 arm64 DMG 以可回滚方式覆盖安装 `/Applications/Y-Clip.app`，确认 `1.0.18 (19)`、Team ID `A94225N8T5`、严格 codesign、stapled ticket、Gatekeeper 与 arm64 thin 架构通过并恢复后台运行；上述 x86_64 最终 DMG 中严格 thin 的 App 通过 Rosetta translated 模式短暂启动冒烟后已终止，镜像和临时挂载点已清理。
- README、AGENTS、AI_CONTEXT 和公开 changelog 已同步为 `v1.0.18` 双架构版本。发布源码提交和 tag `v1.0.18` 已推送；GitHub Draft Release 已严格先上传 arm64、确认其为首个且唯一 DMG 后再上传 x86_64，远端两份资产的名称、顺序、bytes、SHA256 digest 和真实换行 release notes 均与上述最终文件一致。公开前的 Draft 核验阶段未读取或输出公证/GitHub 凭据。
- 完成 `v1.0.18` 正式发布闭环：Release `https://github.com/Rainchen537/Y-Clip/releases/tag/v1.0.18` 已公开；GitHub latest-release API 确认返回 tag `v1.0.18`，且恰好包含 arm64、x86_64 两份 DMG，首个 `.dmg` 仍为 arm64，远端顺序、bytes 与 SHA256 digest 均未变化。远端 tag 解析结果与本地 `853b9ae` 发布源码提交一致；后续维护记录提交不移动 tag。

## 2026-07-21

- 历史面板右上角新增图钉：固定后保持在普通窗口上方，可从标题区域或背景拖动；取消固定后恢复临时面板的 outside-click 关闭行为。
- 新增独立固定面板快捷键，默认 `Option+Shift+Command+V`；普通 `Option+Command+V` 继续在当前输入光标或鼠标附近打开临时面板。两组快捷键使用独立 Carbon ID，录制期间临时注销两组热键，使冲突组合能够被捕获、拒绝并回滚。
- 固定面板 frame 使用 `UserDefaults` 持久化并约束到有效屏幕可见区域；选择历史项后面板继续显示，粘贴前按当前前台 App 刷新 focus context。
- 固定面板接入 `ClipboardHistoryStore.observe`：新复制内容、历史项置顶或清空后实时刷新行列表，按 item ID / dedupe key 尽量保留选择，同时保留窗口上边缘与滚动位置。
- 设置页新增固定面板快捷键录制入口，功能预览同步显示图钉；维护说明中的过度发布规则已改为只发布实际需要分发的 Y-Clip 改动。
- 准备发布 `v1.0.17`，版本号为 `CFBundleShortVersionString=1.0.17`、`CFBundleVersion=18`；README、公开 changelog 和维护说明已同步。
- 最终安装冒烟发现固定面板保持 key window 时会截获自动生成的 `Command+V`；修复为从当前前台 App 自身的 Accessibility 树读取焦点元素，并在固定面板选择历史项后释放 key window，再恢复目标输入控件。受控 NSTextView Probe 验证选择后固定面板继续显示、前台保持目标 App，自动粘贴成功且只记录 `changed length=28`，未输出用户剪贴板内容。
- 重新基于最终修复源码生成正式产物：App 公证 ID 为 `1527e9af-2879-4eef-b064-4bc0a142f329`。DMG 首次提交 ID `7b252706-319a-412a-a98a-1cf51a8ebcf3` 在上传后因连接超时长期停留 `In Progress`，随后对同一本次新生成 DMG 重新提交并以 ID `9512ad29-d062-47ff-83d6-20b1b59707d8` 获得 `Accepted`；未复用旧镜像。
- 最终 `Y-Clip-v1.0.17.dmg` SHA256 为 `1f9a4c0d24d887d5c7c80ff6829532da6cee062a56f0891902cf8022ebc45af8`。DMG、可见 `Y-Clip.app` 和隐藏 `Global Clipboard.app` 兼容副本均通过严格签名、stapled ticket、Gatekeeper 与 `hdiutil verify`。
- 从最终 DMG 覆盖安装 `/Applications/Y-Clip.app`，确认正式安装版为 `1.0.17 (18)`、Bundle ID `com.lixingchen.GlobalClipboard`、`LSUIElement=true`、Developer ID 团队 `A94225N8T5`，且只运行一个正式进程。最终安装版验证两组快捷键分别显示临时/固定状态，图钉 AX 描述正确；固定面板在历史变化后继续显示，选择后仍保持浮层、恢复受控输入焦点并自动粘贴，测试后已恢复用户剪贴板历史和原始剪贴板。
- 完成 `v1.0.17` 发布闭环：对应源码提交 `2897d1e0bbb0d6f6a3c8d3db3c2647b7b8582869`、tag `v1.0.17`、Release `https://github.com/Rainchen537/Y-Clip/releases/tag/v1.0.17`。GitHub Release 标题和真实换行说明正确，资产 `Y-Clip-v1.0.17.dmg` 的远端 digest 为 `sha256:1f9a4c0d24d887d5c7c80ff6829532da6cee062a56f0891902cf8022ebc45af8`，与本地最终焦点修复版 DMG 完全一致；tag 保持指向最终功能源码提交，发布记录使用后续文档提交，不移动 tag。

## 2026-07-20

- 同步新版共享 APFS DMG 框架：背景改为浅色高对比设计并默认生成 `1280×800 / 144 DPI` 的 2x Retina PNG，按 App 与 Applications 的真实 Finder 坐标绘制居中箭头；安装窗口显式隐藏工具栏、状态栏、路径栏和标签页栏，防止标签页栏改变内容区高度并造成图标偏移。
- 背景资源从普通 `.background` 迁移到 Finder 特殊隐藏的 `.Trashes/bg.png`；可写镜像保存布局后清除 `.fseventsd`、索引和临时卷元数据，最终只读镜像会断言这些项目不存在，避免用户开启“显示隐藏文件”时看到框架目录。
- 保留卷根 `Global Clipboard.app` 兼容副本，并在 APFS 上使用 BSD `hidden` 标志隐藏；不再生成 `.hidden` 清单，也不向已签名 App 写入 FinderInfo。兼容副本的 Finder 图标坐标保存到安装窗口画面之外，最终只读重挂载会同时验证画外位置、hidden 标志和严格签名。
- 使用当前正式安装的 Y-Clip App 完成包含 `Global Clipboard.app` 兼容副本的完整测试 DMG：最终 UDZO 镜像重挂载后，两份 App 的严格签名和 hidden 状态、背景物理分辨率/DPI、元数据缺失、窗口栏状态、可见图标与兼容图标完整坐标均通过自动断言；深色系统外观截图确认黑色名称清晰、箭头居中且兼容副本不覆盖布局。
- 准备发布 `v1.0.16`，版本号为 `CFBundleShortVersionString=1.0.16`、`CFBundleVersion=17`；README、公开 changelog 和维护说明已同步。
- 基于最终源码生成并验证本次全新 `Y-Clip-v1.0.16.dmg`：App 公证 ID 为 `95547993-a7e9-4882-876d-57429ba43be2`，DMG 公证 ID 为 `f7fac22d-2849-4b10-a68f-979587ab8528`，SHA256 为 `ce74a206533c7a7ac130e883767ef3649f57fdf4f73dff7529f38a865417749e`。DMG 首次时间戳签名遇到 Apple timestamp 服务瞬时不可用，随后对同一本次新生成镜像重试成功；DMG 与两份 App 的严格签名、stapled ticket、Gatekeeper、BSD hidden 和 `hdiutil verify` 均通过。
- 完成 `v1.0.16` 发布闭环：对应源码提交 `5690b4e4b2eaca11340bbb050cf0111b8023f67a`、tag `v1.0.16`、Release `https://github.com/Rainchen537/Y-Clip/releases/tag/v1.0.16`。GitHub Release 的 tag、标题、真实换行 release notes、资产 `Y-Clip-v1.0.16.dmg`、下载链接和 asset digest 均已复核，远端 digest 与本地 SHA256 完全一致。
- 从上述最终签名、公证并 stapled 的 DMG 覆盖安装到 `/Applications/Y-Clip.app` 并启动；确认正式安装版为 `1.0.16 (17)`、Bundle ID `com.lixingchen.GlobalClipboard`、`LSUIElement=true`、Developer ID 团队 `A94225N8T5`，hardened runtime、严格 codesign、App/DMG stapled ticket 与 Gatekeeper 均通过。
- 最终安装版菜单栏入口、默认 `Option+Command+V`、历史面板及 Esc 关闭已验证；AX tree 同时包含文字和图片历史。使用 disposable NSTextView Probe 选择当前首项后，焦点恢复和自动 Command-V 成功，只记录 `changed length=5334`，未输出剪贴板内容，也未改变持久历史顺序。
- 设置页确认 `Y-Clip v1.0.16`、当前副本为“正式安装版”、辅助功能“已开启”；更新页显示 GitHub Release 渠道并成功确认“已是最新版 1.0.16”。

## 2026-07-19

- 接入仓库内 vendored 的 `Y-Framework/Permission`，把首次启动权限汇总、后续缺失权限提示、状态指纹去重、正式安装副本检查和权限修复动作统一交给共享协调器；实际 Accessibility 查询、定向 TCC 刷新和产品文案仍保留在 Y-Clip。
- 接入仓库内 vendored 的 `Y-Framework/DMG`，`make_dmg.sh` 仅保留产品配置；背景按 Y-Clip 名称动态生成，Finder 布局、`.DS_Store` 背景记录、Applications 链接、签名和最终只读重挂载均由框架验证。
- Y-Clip 的 `Global Clipboard.app` 兼容副本继续保留，并通过卷根目录 `.hidden` 清单隐藏，避免 FinderInfo 扩展属性破坏严格代码签名验证；已通过 Finder 截图确认兼容副本不显示。
- 移除旧的项目专用 `icon/DmgBgGen.swift` 和 `icon/dmg_bg.png`，避免共享框架接入后继续维护重复实现。
- 正式安装版重启改为通过 LaunchServices 打开精确 App 路径，不再使用会强制新实例的 `open -n`。
- 通过共享框架独立编译、Y-Clip 完整构建、DMG 生成与背景/布局截图检查；准备发布 `v1.0.15`，版本号为 `CFBundleShortVersionString=1.0.15`、`CFBundleVersion=16`。
- 完成 `v1.0.15` 发布闭环：源码提交 `149c17dae2cf5c62f4bc81d45db554c9d1c18abe`、tag `v1.0.15`、Release `https://github.com/Rainchen537/Y-Clip/releases/tag/v1.0.15`；最终 `Y-Clip-v1.0.15.dmg` SHA256 为 `233a2942f5fe5d0658bc00633c9708dc41d945c88825f11a1459eab63ff60844`，GitHub asset 名称、下载链接和 digest 已核对一致，App 公证 ID 为 `9c11e076-e648-4491-b02c-42a8ee6d0e3f`，DMG 公证 ID 为 `be64a201-9d86-4911-94d7-2e5e2184ec10`。
- 从上述最终 DMG 覆盖安装到 `/Applications/Y-Clip.app` 并通过 LaunchServices 启动；确认版本 `1.0.15 (16)`、Bundle ID `com.lixingchen.GlobalClipboard`、Developer ID 团队 `A94225N8T5`、hardened runtime、严格 codesign、App/DMG stapled ticket 与 Gatekeeper 均通过，且只运行一个 `GlobalClipboard` 进程。最终 DMG 中可见 `Y-Clip.app` 与隐藏 `Global Clipboard.app` 兼容副本均通过签名验证；安装版权限页显示“正式安装版”和辅助功能“已开启”，设置与更新入口正常，`Option+Command+V` 可打开历史面板，并可用 Enter 选择文本记录后自动粘贴；图像历史行为沿用本轮此前验证结果，最终安装冒烟未额外写入用户的持久剪贴板历史。

## 2026-07-18

- 移除误恢复的旧 `SettingsPopoverController.swift`，消除 `ClipboardPreviewView` 重复声明导致的构建失败。
- 修复功能页旁侧预览的生命周期：功能页保持显示，关闭后以“功能”页重新打开设置时预览会恢复，App 失活后再激活也会恢复。
- 预览可见时移动或缩放设置窗口只重排预览，不再把设置窗口弹回旧位置；切换页面或关闭设置也不再丢失用户选择的 frame。设置窗口与预览会在窄屏上联动缩放，并从上下左右可用区域中选择不越界的位置。
- 返回 App 时自动刷新辅助功能状态；权限记录重置改为后台执行，并统一复用会清理 `accessibilityWasTrusted` 的修复流程。
- 权限页新增运行副本诊断，按 `/Applications/Y-Clip.app` 路径、Bundle ID、代码签名有效性和 Developer ID 团队标识区分“正式安装版”与“开发副本”，并可切换到验证通过的安装版。
- App 内和 `reset_accessibility.sh` 的 TCC 刷新均限定为 `Accessibility` 服务与 `com.lixingchen.GlobalClipboard`，不会全局重置其他 App；开发副本发起修复后不会再次请求该副本的 TCC，若有效正式版存在会切换过去并在那里引导授权。
- 自动更新安装前使用 `YSettingRuntimeIdentity` 拒绝 symlink、错误 Bundle ID、错误团队、非 Developer ID Application 或未启用 hardened runtime 的 App，同时保留 DMG/App 的 codesign 与 Gatekeeper 验证，且不再通过清除 quarantine 代替验证。
- `release.sh` 移除 Apple ID 字面量，改用环境变量和 notary profile 指引；打包前验证 App 的 stapled ticket，最终 DMG 挂载后验证可见 `Y-Clip.app` 与隐藏 `Global Clipboard.app` 兼容副本。
- 共享设置框架增加屏幕可见区域约束和 sidebar identifier 校验；关于页补充产品身份。
- 准备发布 `v1.0.14`，版本号为 `CFBundleShortVersionString=1.0.14`、`CFBundleVersion=15`，公开 changelog 日期为 `2026-07-18`。
- 完成 `v1.0.14` 发布闭环：源码提交 `3492950493a1aea1dd3d830c97a888f32e219d9f`、tag `v1.0.14`、Release `https://github.com/Rainchen537/Y-Clip/releases/tag/v1.0.14`；最终 `Y-Clip-v1.0.14.dmg` SHA256 为 `2bf907ade8bec760fc55708a3e55e5cc5aea856b8e85bf6b30b6d06c3200ff28`，GitHub asset digest 已核对一致。
- 从上述最终 DMG 覆盖安装到 `/Applications/Y-Clip.app` 并通过 LaunchServices 启动；确认版本 `1.0.14 (15)`、Bundle ID `com.lixingchen.GlobalClipboard`、Developer ID 团队 `A94225N8T5`、hardened runtime、严格 codesign、App/DMG stapler 与 Gatekeeper 验证通过。安装版权限页截图确认“正式安装版”和辅助功能“已开启”，且只运行一个 `GlobalClipboard` 进程。

## 2026-07-13

- 新增项目级 `AGENTS.md`，继承 Y-Project 根规范，并记录 Y-Clip 的仓库、分支、版本字段、DMG 命名和本机安装验证要求。
- 将“每次修改任务必须发布新版本”设为强制完成条件：签名、公证并 staple DMG，发布 GitHub Release，再从最终 DMG 覆盖安装本机 App。

## 2026-07-10

- 将 GitHub 仓库从 `Rainchen537/global-clipboard` 重命名为 `Rainchen537/Y-Clip`。
- 同步更新本地 `origin`、README 仓库记录与克隆命令、项目主页链接及 GitHub Release 自动更新接口。

## 2026-06-28

- 接入 `Y-Framework/Setting` 共享设置页面框架。
- 在仓库内 vendored `Y-Framework/Setting/YSettingsFramework.swift`，避免单独 clone GitHub 仓库时缺少父级共享框架。
- 将菜单栏入口改为简单菜单：打开设置、更多 Y-Project 占位和退出项。
- 将原菜单栏 popover 设置页重构为独立设置窗口，采用左侧 sidebar + 右侧内容区结构。
- 将设置页 sidebar 统一为 `通用 / 功能 / 权限 / 更新 / 关于`，旧业务分类移入右侧 section card。
- 调整共享设置框架窗口比例、sidebar 宽度、内容内边距和卡片等宽约束；通过截图检查 Y-Clip 功能页旁侧预览与设置窗口没有遮挡。
- 将共享设置窗口默认尺寸放大到 `1200 x 810`，并把左侧导航按钮改为居中排列；通过截图复查 Y-Clip 功能页和旁侧预览。
- 修正 sidebar 内 App 信息区和导航区的居中方式，改为直接按 sidebar 中心线约束；移除设置页页面标题和 section card 内的静态解释性小灰字。
- 将 sidebar App 信息区顶部间距调为 96，让 logo 保持在左侧上半部分。
- 拆出 `ClipboardPreviewView.swift`，保留剪贴板面板设置的旁侧预览；设置窗口左右两侧哪边空间足够就显示在哪边。
- 更新 `build.sh`，编译时加入仓库内 `Y-Framework/Setting/*.swift`，并保留父级共享框架回退路径。
- 准备发布 `v1.0.13`，版本号更新为 `CFBundleShortVersionString=1.0.13`、`CFBundleVersion=14`，并新增用户可见 `CHANGELOG.md`。
- 发布 `v1.0.13` 的签名、公证、staple 后 DMG，SHA256：`4bc5fe2fd8f6bb922677497917c26e2db0496c7e9d61cb6ead3905a8f9f3e32b`。

## 2026-06-27

- 统一为 Y-Project 标准 AI 维护结构：`AI_CONTEXT.me` 负责工作规范和项目说明，`CHANGELOG.me` 负责维护更新记录。
- 在 `AI_CONTEXT.me` 中引用根目录 `Y_PROJECT_APP_STANDARD.me`，并补充 GitHub MCP / GitHub App release、签名公证、本机安装验证等通用要求。

## 2026-06-24

- 修复剪贴板历史面板在超时或新内容后回到顶部时可能停在空白区域的问题。现在会按第一条真实内容计算顶部滚动位置，并在 panel 显示后的下一轮布局再次校准。
- 进一步定位到普通 `NSStackView` 作为 scroll document view 时会产生负 y frame，导致滚动条看似在顶部但内容不在可视区；历史列表改用顶部原点的 `TopAlignedStackView`。
- 发布 `v1.0.12`，DMG SHA256：`034e3d3c8d80c172c31204eab3f1bb3b3df79229d95e564538ec0ea4ecaa9904`。

## 2026-06-22

- 将对外软件名从 Global Clipboard 改为 Y-Clip。
- 保留内部 Bundle ID `com.lixingchen.GlobalClipboard`、可执行名 `GlobalClipboard` 和历史数据目录，避免影响辅助功能权限、自动更新和历史记录。
- 保留新的菜单栏线性描边小图标。
- 规划 DMG 内隐藏 `Global Clipboard.app` 兼容副本，兼容旧版自动更新器。
- 构建脚本会清理 `build/Global Clipboard.app` 旧名残留，只保留 `build/Y-Clip.app`。
- 新增 `AI_CONTEXT.me`，记录项目介绍、构建流程、发布流程和本地更新日志约定。
- 发现本机启动台仍显示旧名时，实际是 `/Applications` 仍安装旧包；已安装新版，并让 `install_app.sh` 安装后刷新 LaunchServices。
