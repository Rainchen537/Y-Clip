<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      Y-Clip — hardened runtime entitlements

      本 app 的能力全部依赖「辅助功能」运行时授权，无需在此声明：
        · 自动粘贴：CGEvent 键盘事件注入（PasteController）
        · 全局热键：Carbon RegisterEventHotKey（HotKeyController）
        · 事件监听：NSEvent 全局 monitor（ClipboardPanelController）
        · 开机自启：SMAppService（LaunchAtLoginController）

      不使用 App Sandbox；自动更新器仅通过网络访问 GitHub Release；不加载第三方动态库。
      网络访问在非沙盒 App 中不需要额外 entitlement。保持最小集即可，不启用任何削弱 hardened runtime 的项，
      以获得最顺畅的公证（notarization）结果。
    -->
</dict>
</plist>
